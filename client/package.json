{
  "name": "text-me-secretly-client",
  "version": "0.1.0",
  "description": "Privacy-first P2P messenger — Vanilla JS + Capacitor shell.",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "copy": "cap copy",
    "build:noise": "esbuild build/noise-entry.mjs --bundle --format=esm --platform=browser --legal-comments=none --banner:js=\"/* GENERATED by 'npm run build:noise' from build/noise-entry.mjs — do not edit. Bundles noise-handshake (Noise_XX_25519_ChaChaPoly_BLAKE2b) + sodium-javascript. */\" --outfile=www/js/vendor/noise-xx.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "vpn-detector": "file:../plugins/vpn-detector"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "esbuild": "^0.23.0",
    "noise-handshake": "^4.2.0",
    "sodium-javascript": "^0.8.0"
  }
}
